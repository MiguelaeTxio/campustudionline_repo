# /home/MiguelAeTxio/CampuStudiOnline/messaging/routing.py

# This file has been intentionally emptied.
# The real-time messaging logic has been migrated from WebSockets (which require ASGI)
# to an HTTP Polling system (compatible with WSGI).
# The WebSocket routes defined here are no longer necessary for this functionality.

websocket_urlpatterns = [
    # No active WebSocket routes for the 'messaging' app at this time.
]
